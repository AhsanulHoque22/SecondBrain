requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
playwright>=1.44
pytest>=8.0

# Optional: loads a local .env file automatically (see .env.example).
# Without it, set environment variables directly instead.
python-dotenv>=1.0

# LLM provider SDK — install the one(s) matching your LLM_PROVIDER (single-
# backend mode) or LLM_BACKEND_CHAIN (multi-backend mode) setting. Only
# three vendor SDKs exist here regardless of how many backends you chain —
# groq and deepseek both reuse the `openai` SDK (family="openai" in
# llm_providers.BACKENDS, just a different base URL/key). Uncomment (or pip
# install separately) whichever families you actually use; the rest are
# never imported so they don't need to be installed.
anthropic>=0.116       # backend "anthropic" (LLM_PROVIDER default)
openai>=1.50            # backend "openai", and also "groq" / "deepseek" (same SDK, different base_url/key)
google-generativeai>=0.8   # backend "google"
